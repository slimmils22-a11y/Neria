# Constats de la campagne de tests fonctionnels — à arbitrer

Liste vivante, alimentée par chaque phase. **Rien n'est corrigé sans décision de l'utilisateur.**
Statuts : `À arbitrer` · `Corriger` · `Assumé` (décision de ne pas corriger, avec la raison) · `Corrigé` (commit).

| N° | Phase | Constat | Gravité | Preuve | Statut |
|---|---|---|---|---|---|
| F-001 | P2a | L'attribut `lang` est absent de la balise `<html>` de **tous** les e-mails (`layout.html` ne pose que `dir`). Conséquences : lecteurs d'écran qui prononcent mal, correcteur orthographique et « traduire ce message » de Gmail moins fiables, critère WCAG 3.1.1. Correctif simple : injecter `lang` comme `neria_dir`. | Moyenne (accessibilité, i18n) | `results/P2a_render.json` — 2 223 / 2 223 combinaisons | Corrigé (20/09/2026) |
| F-002 | P2a | Contraste insuffisant (WCAG AA 4,5:1) : couleur d'accent `#b38b59` sur blanc = **3,11:1** (liens, pied de page, « Gérer mes préférences ») ; gris `#8c857e` = **3,64:1** (en-têtes de tableau). Compromis de charte à trancher : assombrir légèrement l'accent pour le texte (garder l'accent actuel pour les filets/pastilles) ou assumer. | Moyenne (accessibilité, charte) | `results/P2a_render.md` › Contrastes | Corrigé (21/09/2026) |
| F-003 | P2a (analyse statique) | Boutons non « bulletproof » pour Outlook bureau : `a.neria-btn` s'appuie sur `padding`/`background-color` de la balise `<a>`, que le moteur Word ignore en partie ; le layout n'a aucun bloc `<!--[if mso]>` ni VML. Risque : bouton réduit à un fond derrière le texte. **À confirmer par une capture Outlook réelle** (décision « source de rendu Outlook » attendue). | Moyenne (rendu) | Analyse de `layout.html` | Assumé (21/09/2026) — rendu correct dans le nouvel Outlook de Windows 11 (clair et sombre, captures utilisateur) ; Outlook classique (moteur Word) en fin de vie et aucun rendu réel disponible ; à rouvrir si un client s'en plaint |
| F-004 | Cadrage | Compatibilité annoncée `config.xml` PS ≥ 8.0.0 (PHP 7.2+) mais le code utilise `match` (9 fois) et `?->` (2 fois), invalides avant PHP 8.0. Lint PHP 7.4 à faire pour confirmer (décision « PHP 7.4 portable » attendue). | Haute si confirmé (fatal sur PS 8.0/8.1 en PHP 7.4) | grep du code | Corrigé (21/09/2026) — confirmé par php 7.4 -l : 7 fichiers fatals, réécrits |
| F-005 | P1 | Action BO `search_customers` sans aucun bouton/script qui la déclenche : code mort probable (l'interface utilise `customer_autocomplete`). | Faible | `INVENTORY.md` | À arbitrer (P8) |
| F-006 | P2a | Sujet de repli : 4 templates n'ont pas de clé `greeting_main` (complete_your_look, collection_completion, return_slip, neria_fallback). Leur sujet doit être fourni par leur gestionnaire — **à vérifier à l'envoi réel** (P3/P4) ; sinon sujet vide/générique en envoi manuel. | Faible à moyenne | `results/P2a_render.json` › R090 | À vérifier |
| F-007 | P2b (capture Outlook 21/09) | Les liens du pied de page ignorent le réglage Design « Texte du pied de page » : la règle CSS `.neria-footer a` (gris, souligné) n'est jamais appliquée par l'inliner, seule la règle globale `a` (accent assombri, sans soulignement) l'est — vérifié dans le HTML envoyé (`color: #906f46; text-decoration: none`). Rendu actuel lisible (F-002) ; seule la cohérence avec le réglage est en cause. Corriger = liens gris soulignés (changement d'apparence) ou supprimer la règle morte. | Faible (cosmétique) | HTML rendu de unboxing_guide | Corrigé (21/09/2026) — réglage « Couleur des liens » ajouté, les liens du pied de page le suivent |
| F-008 | P8a | 21 avertissements PHP/Smarty distincts à l'ouverture des onglets du back-office (variables ou clés non définies : `automations` calc_only, `stats` spf/dkim/dmarc/bimi/bl, `help` diagnostic, `customer_history`, `translations` template/langue sélectionnés) + modificateur `\|abs` déprécié + libellé « ⚙ calcul » codé en français. Invisibles hors mode debug PrestaShop, mais journalisés à chaque affichage. | Faible à moyenne (journaux, mode debug) | `results/P8a_tabs.md` | Corrigé (21/09/2026) |
| F-009 | P8a | L'inventaire P1 omettait 28 actions du back-office réellement gérées (onglet Traductions entier : `$tradAction === '…'`, listes `in_array`) : la matrice annonçait 139 actions alors qu'il y en a 167. Détecteur étendu, 28 lignes ajoutées à la matrice (856 lignes). Le garde-fou de couverture (test_817) ne pouvait pas le voir : il compare la matrice au détecteur, pas le détecteur au code. | Moyenne (fiabilité de la campagne) | `build_inventory.php`, écart trouvé par R121 du parcours des onglets | Corrigé (21/09/2026) |
| F-010 | P8a | Le diagnostic du Centre d'aide enregistre ses résultats **déjà rédigés** dans la langue de l'employé qui l'a lancé (`CONFIG_RESULTS`, JSON de textes) : un autre employé d'une autre langue les voit en français (ou autre) jusqu'au prochain lancement. Correction possible : enregistrer clé + variables et traduire à l'affichage. | Faible | R112 sur l'onglet Aide (8 messages) | À arbitrer |

## Pas des défauts (vérifiés et écartés)

- Liens `#`, images relatives, « français résiduel » (messages factices d'aperçu), variables non résolues : artefacts du jeu de données d'aperçu, corrigés dans le harnais `render_all_templates.php` (le détecteur a été validé sur un template piégé : R010, R011, R020, R021, R040, R050, R051, R070, R080 se déclenchent bien).
